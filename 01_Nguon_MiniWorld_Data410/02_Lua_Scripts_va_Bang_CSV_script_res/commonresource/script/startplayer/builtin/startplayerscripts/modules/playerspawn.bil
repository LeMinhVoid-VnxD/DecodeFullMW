
local Players = game:GetService('Players')
local PlayerSpawn = CommonModule.Class("PlayerSpawn")

function PlayerSpawn:OnInit()
	-- 角色的默认模型设置目前在 c++ 设置
	-- 在此处设置会导致开发者逻辑比这个早执行导致开发者逻辑设置的被覆盖
	-- local WorkSpace = game:GetService("WorkSpace")
	-- local localplayer = Players.LocalPlayer
	-- Players.PlayerAdded:Connect(function( player )
	-- 	if player == localplayer then 
	-- 		WorkSpace:WaitForChild(localplayer.Name)
	-- 		local localCharacter = player.Character
	-- 		localCharacter.ModelId = "sandboxSysId://entity/player/player01/body.omod"
	-- 	end
	-- end)
end

return PlayerSpawn.New()

